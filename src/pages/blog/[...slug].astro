import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';

export async function getStaticPaths() {
  const posts = await getCollection('blog');
  return posts.map(post => ({
    params: { slug: post.slug },
    props: { post },
  }));
}

const { post } = Astro.props;
const { Content } = await post.render();

// Function to format date (same as in index)
function formatDate(date) {
    return new Date(date).toLocaleDateString('en-US', {
        year: 'numeric',
        month: 'short',
        day: 'numeric'
    });
}

<BaseLayout title={post.data.title}>
    <article class="blog-post">
        <h1>{post.data.title}</h1>
        <p class="post-meta">
            <span>Published on {formatDate(post.data.pubDate)}</span>
            {post.data.tags && (
                <span class="tags"> | Tags: {post.data.tags.join(', ')}</span>
            )}
        </p>
        <hr>
        <div class="post-content">
            <Content />
        </div>
    </article>
</BaseLayout>

<style>
    .blog-post {
        max-width: 800px; /* Adjust content width */
        margin: 0 auto;
    }
    .post-meta {
        font-size: 0.9em;
        color: #ccc;
        margin-bottom: 1em;
    }
    .tags {
        font-style: italic;
    }
    hr {
        border: 0;
        height: 1px;
        background: #444;
        margin: 1.5em 0;
    }
    .post-content {
        line-height: 1.7;
    }
    /* Add styles for Markdown elements if needed */
    .post-content h2 {
        font-size: 1.4em;
        margin-top: 1.5em;
        margin-bottom: 0.5em;
        border-bottom: 1px solid #555;
        padding-bottom: 0.2em;
    }
    .post-content p {
        margin-bottom: 1em;
    }
    .post-content ul, .post-content ol {
        list-style: initial;
        margin-left: 1.5em;
        margin-bottom: 1em;
    }
    .post-content code {
        background-color: #3a3a3a;
        padding: 0.2em 0.4em;
        border-radius: 3px;
        font-size: 0.9em;
    }
    .post-content pre {
        background-color: #2a2a2a;
        padding: 1em;
        border-radius: 4px;
        overflow-x: auto;
        margin-bottom: 1em;
    }
    .post-content pre code {
        background-color: transparent;
        padding: 0;
        font-size: inherit;
    }
</style> 